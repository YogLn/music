import React, { memo } from 'react'

export default memo(function TopRanking(props) {
	const { info } =  props
  const { tracks = [] } = info;

	// redux hooks

	// hooks
	
	return (
		<div>
			
		</div>
	)
})
