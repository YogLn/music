import React, { memo } from 'react'

import { getSizeImage } from '@/utils/format-utils';

import { AlbumWrapper } from './style'

export default memo(function AlbumCover(peops) {
	const { info, size = 130, width = 153, bgp = "-845px"  } = props
	return (
		<AlbumWrapper>
			
		</AlbumWrapper>
	)
})
